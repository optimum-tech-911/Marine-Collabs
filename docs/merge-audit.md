# Merge audit

The root application supplied stronger verified creator image and evidence storage. V5 supplied the selected product architecture: its 12-creator commercial roster, guided matching, persistent shortlist, comparison, campaign brief, accessibility primitives, route metadata, static shell generation and Cloudflare release material are retained.

Both applications use the same pinned React, Vite and TypeScript stack. V5's structure was selected because it has one complete product flow and validation scripts; it is now promoted into the root unified application, while the nested V5 folder remains an untouched source reference.
